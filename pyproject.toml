[project]
name = "gltf2bb"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
gltf2bb = "src.inspect:main"

[build-system]
requires = []
build-backend = "build_backend"
backend-path = ["."]
